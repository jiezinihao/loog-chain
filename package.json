{
  "name": "think-chain",
  "version": "0.1.0",
  "private": true,
  "description": "个人前端项目展示的 pnpm monorepo。",
  "packageManager": "pnpm@10.34.4",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @think-chain/entry dev",
    "build": "pnpm --filter @think-chain/entry build",
    "typecheck": "pnpm --filter @think-chain/entry typecheck"
  }
}
